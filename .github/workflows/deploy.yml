name: Deploy to Streamlit Cloud

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Trigger Streamlit Cloud deployment
      run: echo "Push to main branch triggers automatic deployment on Streamlit Cloud"
